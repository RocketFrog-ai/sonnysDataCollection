"""Google Places helpers. `nearby_competitors` anchors the Explore-markets Competition Coverage
insight on real nearby car washes; `search_nearby` is its Places (New) searchNearby wrapper.

Lived under app/site_analysis/features/ until 2026-07. That whole subsystem was removed; these two
modules were its only survivors, because the Streamlit UI still needs them.
"""
