# Pinned to the versions this app is actually tested against, because loose
# ranges bit us: an image built with `streamlit>=1.48,<2` resolved streamlit
# 1.61 + starlette 1.4, whose gzip middleware returns 500 for any request
# carrying `Accept-Encoding: gzip` — i.e. every browser request, while curl
# (which sends none) worked fine. Bump these deliberately, and re-test in a
# browser, not just with curl.
streamlit==1.58.0
starlette==1.3.1        # streamlit subclasses its GZipMiddleware
uvicorn==0.51.0
pandas==3.0.2
numpy==2.4.4
plotly==6.6.0
vaderSentiment==3.3.2
