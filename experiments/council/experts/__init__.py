"""Committee experts — Historical · Competition · Local-Market · Capacity · Finance.

`build_experts(order)` (added alongside the concrete expert modules) returns the seated experts in
investigation order, Finance last. `Expert` is the base contract in `base.py`.
"""
from experiments.council.experts.base import Expert  # noqa: F401
